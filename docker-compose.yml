version: '3.8'

services:
  react-portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: react-portfolio-app
    restart: unless-stopped
    networks:
      - portfolio-network

networks:
  portfolio-network:
    driver: bridge
